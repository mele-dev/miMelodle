{
    "transforms": [{
        "mode": "sql",
        "include": "**/*.sql",
        "emitTemplate": "{{dir}}/{{name}}.queries.ts"
    }],
    "srcDir": "./src/queries/",
    "failOnError": true,
    "camelCaseColumnNames": true,
    "db": {
        "host": "localhost",
        "user": "admin",
        "dbName": "melodle",
        "password": "admin",
        "port": 5434
    }
}
